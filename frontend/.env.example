# Base URL of the SolarHand backend API.
# Dev: point at the locally running FastAPI server (CORS is enabled there).
# Prod: leave empty to call the same origin (the reverse proxy forwards to the API).
VITE_API_BASE=http://localhost:8000
