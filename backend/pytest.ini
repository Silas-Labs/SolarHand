[pytest]
# Put the backend/ dir on sys.path so `import app` and `import tests.*` both
# resolve when pytest is run from backend/. Requires pytest >= 7.
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = *Tests Test*
python_functions = test_*
addopts = -ra
